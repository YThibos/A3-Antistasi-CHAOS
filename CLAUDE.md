<!--
    This file intentionally only points at AGENTS.md, which holds the actual contract.

    It should be a symlink (`ln -s AGENTS.md CLAUDE.md`), but creating symlinks on this
    Windows workstation needs Developer Mode plus `git config core.symlinks true`.
    Until then the @-import below gives the same effect: Claude Code loads AGENTS.md
    in full whenever it loads this file.
-->

@AGENTS.md
